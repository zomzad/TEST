var _formElement;

function testForm_onLoad(formElement) {
    _formElement = formElement;
}

//字串替換
$(this).attr('name', $(this).attr('name').replace(/\[\d+\]/g, "[" + idx + "]"));

//找尋第一個
tr.find("td:eq(0)");

//字串編碼
encodeURI($.trim(field.find('input[name$="UserID"]').val()));

//正規
day.match(/^\d*(\.\d)?$/); //
srcElement.value.match(/^(([0-1]?[0-9])|([2][0-3]))([0-5]?[0-9])?$/);

//包含在array
$.inArray($('#Schedule').val(), ['8', '9']);

//停用
$('input:text,select').attr({ 'disabled': false })